<h2 class="field">New Bird</h2>

<% if user_signed_in? %>

<%= render 'form' %>

<% else %>

<p id="notice">Please <%= link_to('log in', new_user_session_path) %> to enter a bird. </p>

<%end%>

<%= link_to 'Back', birds_path %>
